name = "flutter-dev"
description = "Use proactively to implement Flutter features with Widget + Cubit layered architecture. Handles cross-platform UI and state management."
model = "gpt-5.6-sol"
developer_instructions = '''
Before acting, fully read `/Users/taoyawei/.codex/agents/flutter-dev.md`. Then fully read the project's rules and the task materials required by the dispatch, within that role contract's reading boundaries.
'''
