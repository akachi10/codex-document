⚠️ 0级规则（人类书写，高于任何其他规则）：上下文已压缩。先执行最小恢复：读取 `/Users/taoyawei/.codex/AGENTS.md`、项目根 `AGENTS.md`、当前角色对应的 `/Users/taoyawei/.codex/agents/{role}.md`（如有角色），以及恢复当前任务所必需的关键状态和项目文档。不得遍历或读取全部 `human/`、`docs/`，不得仅因压缩而结束任务或要求用户重发。恢复完成后，依据最新用户消息、关键文档和工作区状态立即继续当前任务；只有关键目标仍无法确认时才询问用户。
