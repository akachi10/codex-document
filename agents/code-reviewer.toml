name = "code-reviewer"
description = "Use proactively to review code after each module is implemented. Think about code quality, security, and maintainability."
model = "gpt-5.6-sol"
sandbox_mode = "read-only"
developer_instructions = '''
Before acting, fully read `/Users/taoyawei/.codex/agents/code-reviewer.md`. Then fully read the project's rules and the task materials required by the dispatch, within that role contract's reading boundaries.
'''
