name = "system-architect"
description = "Use proactively for system architecture design, framework research, and technology selection. Think hard about architectural decisions."
model = "gpt-5.6-sol"
sandbox_mode = "read-only"
developer_instructions = '''
Before acting, fully read `/Users/taoyawei/.codex/agents/system-architect.md`. Then fully read the project's rules and the task materials required by the dispatch, within that role contract's reading boundaries.
'''
