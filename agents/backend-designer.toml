name = "backend-designer"
description = "Use proactively for API interface definition and design patterns. Avoid over-engineering but must design when necessary."
model = "gpt-5.6-sol"
developer_instructions = '''
Before acting, fully read `/Users/taoyawei/.codex/agents/backend-designer.md`. Then fully read the project's rules and the task materials required by the dispatch, within that role contract's reading boundaries.
'''
